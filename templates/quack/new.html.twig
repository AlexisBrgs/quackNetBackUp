{% extends 'base.html.twig' %}

{% block title %}New Quack{% endblock %}

{% block body %}
    {{ form_start(form) }}
    <div class="card bg-secondary border-success w-75 mx-auto my-4 ">
        <h1 class="m-2">Create new Quack</h1>
        <div class="card-title m-2">

        </div>
        <div class="my-4 mx-2">
            {{ form_widget(form) }}
        </div>
        <div class="my-4 mx-2">
            <a class="btn btn-info" href="{{ path('quack_index') }}">Back to list</a>
            <button class="btn btn-success mr-2">{{ button_label|default('Save') }}</button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
